<div class="projDisplay">
  <div class="projDisplayText">
    <h1 class="project-info">{{project.ptitle}}</h1>
    {{project.pdesc}}
  </div>
  <img src="{{project.purl}}" alt="{{project.purl}}" id="pid-img" />
</div>